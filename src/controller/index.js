const userController = require('./user.controller');
const categoryController = require('./catecory.controller');
const postController = require('./post.controller');

module.exports = {
  userController,
  categoryController,
  postController,
};